# Contributing

This repository is the Mintlify site for **TryPost Connect**.

- Work on `main`.
- Match the live contract in `~/Herd/trypost-connect` (`routes/api.php`, MCP server).
- Preview with `mint dev`. Check links with `mint broken-links`.
